server:
  port: 18081

spring:
  config:
    import: optional:file:.env[.properties]
#  cache:
#    type: redis
#    cache-names: redis-cache
#  data:
#    redis:
#      host: localhost
#      port: 16379
#      password: ${REDIS_PASSWORD}
#    mongodb:
#      host: localhost
#      port: 27017
#      database: ${MONGO_DATABASE}
#      username: ${MONGO_USERNAME}
#      password: ${MONGO_PASSWORD}

#  TODO менять на монго!
  datasource:
    url: jdbc:h2:file:./dev_data/giving_db
    username: sa
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate