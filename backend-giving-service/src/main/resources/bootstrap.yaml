spring:
  application:
    name: backend-giving-service
  cloud:
    config:
      uri: http://localhost:18001
      fail-fast: true
      username: user
      password: parole

# TODO вроде это только для git конфигов
#  config:
#    import: optional:configserver:http://localhost:18001